import { Header } from "antd/es/layout/layout";

const Navbar = () => {
  return (
    <Header
      style={{
        background: "#444",
        color: "white",
        fontSize: "1.2rem",
      }}
    >
      <div>Golrang System Project</div>
    </Header>
  );
};

export default Navbar;
